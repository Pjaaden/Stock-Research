# 试运行：本地化迁移 + GitHub 连通性测试（2026-08-26）

- 测试日期：2026-08-26
- 本地化位置：`/Volumes/TiPlus71001TB/Users/zhijian/Documents/Trae/Stock-Research`
- 用途：验证 GitHub 访问输入/输出，并将项目与规则文件本地化至本机文件库「Trae」，便于后续迁移

## 1. GitHub 访问测试

### 读取（Read）—— ✅ 通过

- 克隆仓库 `https://github.com/Pjaaden/Stock-Research` 成功（含全部公司目录、data/*.json、Investment_OS）
- GitHub API 读取成功（最近提交 `88ba6ae6`：Add master file index & multi-page task workspace）

### 写入（Write / Push）—— ✅ 通过（SSH 通道）

| 通道 | 结果 | 说明 |
|---|---|---|
| HTTPS（无凭证） | ❌ | `could not read Username ... terminal prompts disabled`，与 2026-07-21 旧报告一致，需 PAT |
| SSH（本机 key） | ✅ | `git push --dry-run ssh HEAD:refs/heads/trae_local_test` → `* [new branch]`，服务端接受写入（dry-run 未实际创建） |

结论：本机 SSH key（`~/.ssh/id_ed25519`）已具备完整读写权限；HTTPS 通道需按规则在 `TRAE_LOCAL/GITHUB_PAT.local` 提供 PAT（记录到期日 2026-10-21）。

## 2. 工作流与数据完整性

- ✅ 核心规则/工作流文件齐全（PROJECT_INSTRUCTIONS / WORKFLOW / OUTPUT_RULES / GITHUB_CREDENTIAL_POLICY / WORKBENCH_AND_DATA_SCOPE / push_with_local_pat.sh）
- ✅ `data/companies.json` / `watchlist.json` / `logs.json` 均为有效 JSON（companies 9 条、watchlist 6 条）
- ✅ 9 个 `research_folder` 与仓库真实目录全部一致（华润万象生活 = `华润万象生活_01209HK`，已修正）
- ✅ 本仓库无 GitHub Actions（`.github/` 不存在），"工作流" = `TRAE/WORKFLOW.md` 执行流程 + 推送脚本

## 3. 本地化动作

| 动作 | 路径 | 说明 |
|---|---|---|
| 克隆仓库 | `Stock-Research/` | 完整代码与资料落盘 |
| 规则本地化 | `.trae/rules/*.md` | TRAE 自动加载的项目规则（副本，源头为 `TRAE/*.md`） |
| 凭证目录 | `TRAE_LOCAL/` | git 忽略；`GITHUB_PAT.local` 待创建（或直接用 SSH） |
| 工作区目录 | `pages/` | git 忽略；多页面任务工作区 |
| 忽略规则 | `.gitignore` | 保护 `TRAE_LOCAL/` 与 `pages/` 不入 git |
| SSH 远端 | `git remote add ssh` | 本机免 PAT 推送通道（仅本地配置） |

## 4. 迁移提示

- 迁移 = 整体复制 `Stock-Research/` 文件夹（`.trae/rules/` 随目录携带，新机器克隆后执行 `cp TRAE/*.md .trae/rules/` 亦可恢复）
- `TRAE_LOCAL/` 需单独复制（含 PAT 明文，勿走 git）
- 推送通道建议：本机用 `ssh`（已验证），跨机/CI 用 HTTPS+PAT（按凭证规则）
