# 会话交接档案：工作流优化与多设备协作（2026-08-27）

## 基本信息

- 日期：2026-08-27
- 设备：机器B（当前机器，`/Users/Shared/Projects/Stock-Research`）
- 任务类型：系统优化

## 目标

解决双机（机器A / 机器B）通过 git 同步时 TRAE 对话无法跨机同步的问题：新增可同步的“交接档案”层，把会话里的推理、结论、待办沉淀为仓库历史；并补充多设备协作规范与工作流指导。

## 新事实

- TRAE 对话记录存在各机器本地，git 无法同步；`pages/`（草稿区）与 `TRAE_LOCAL/`（凭证）按设计不入库。
- 机器B现状：仅 HTTPS origin、无 `TRAE_LOCAL/` → 只拉不推；但本机 `~/.ssh/id_ed25519` 已生成并已在 GitHub 注册（`ssh -T git@github.com` 返回 `Hi Pjaaden!`）。
- 机器B存在全局规则 `url.https://github.com/.insteadof git@github.com:`，会把 SSH 风格 URL 改写为 HTTPS，SSH 推送前需处理（unset 或改用 ssh remote）。
- 机器A：SSH key + `TRAE_LOCAL/GITHUB_PAT.local`（PAT 有效期记录 2026-10-21）均可推。

## 结论与旧观点修正

- 确认“只拉不推”不是异常，而是 git 忽略设计（`TRAE_LOCAL/`、`pages/` 不入库）导致的正常克隆状态；仅在本机需要推送时才需补凭证。
- 确立历史分层：研究报告（论证）→ `TRAE/sessions/`（进展/待办）→ `data/logs.json`（关键动作）→ `INDEX.md`（入口导航），全部入库可跨机继承。

## 未决问题 / 待验证

- 本机推送凭证最终选择（SSH 优先，或 PAT 备用）——待用户确认后执行并验证首次 push。
- 机器B是否也需要 `TRAE_LOCAL/GITHUB_PAT.local` 作为备用通道——待用户决定。

## 下一步（换机接续点）

1. 用户确认凭证方案后：执行 `git remote set-url origin git@github.com:Pjaaden/Stock-Research.git`（或删除全局 insteadOf 规则），`git pull --rebase` 后首次 `git push origin main` 验证。
2. 日常双机流：开工 `pull --rebase` → 读 `HANDOFF.md`/`sessions/` → 干活 → 收工写 sessions + 更新 HANDOFF → push。
3. 第三台设备（将来）：clone 即完整继承（`.trae/rules/` 已入库），仅需按设备准备凭证。

## 涉及文件

- 新建：`TRAE/QUICKSTART.md`（日常操作速查卡）、`TRAE/sessions/_模板.md`、`TRAE/sessions/20260827_工作流优化与多设备协作.md`（本篇）、`TRAE/HANDOFF.md`
- 修改：`TRAE/WORKFLOW.md`（多设备协作章节 + 读旧资料补充）、`.trae/rules/WORKFLOW.md`（同步副本）、`INDEX.md`（最近动态/导航/维护规则）、`README.md`（工具表/目录树/页脚）、`PASTE_INTO_TREA_STOCK_RESEARCH.md`（主提示步骤2）、`data/logs.json`（登记本条）
